# 目标功能重构 — 产品文档 & 开发计划

## 一、原型图分析

### 原型图1：目标列表页（目标页面.png）
- 顶部标题栏：「目标」+ 「新建目标」按钮
- 目标以卡片列表展示，每个卡片包含：
  - 左侧颜色竖条标识
  - 目标名称（可点击进入详情）
  - 目标周期标签（如 "Q2 2026"、"年度"）
  - 整体进度百分比 + 进度条
  - 关键结果(KR)摘要：每个KR一行，显示类型图标、KR名称、进度条、当前值/目标值
  - KR类型用不同图标区分：📊进度型、✅完成/未完成型、📋任务型
- 支持子目标层级缩进展示
- 空状态：居中图标 + 提示文字

### 原型图2：目标详情页（目标页面 点击目标后显示页面图.png）
- 顶部：返回箭头 + 颜色标识 + 目标名称（可编辑）+ 周期 + 操作按钮（归档/删除）
- 进度总览卡片：大号进度条 + 百分比
- 关键结果区域：
  - 标题「关键结果」+ 「添加KR」按钮
  - 每个KR卡片：
    - 类型标识图标（进度/完成/任务）
    - KR标题 + 权重
    - 进度条 + 当前值/目标值 + 单位
    - 进度颜色根据时间-进度对比：绿色(超前)、黄色(略慢)、红色(严重滞后)
    - 操作：Check-in（更新进度）、编辑、删除
  - KR三种类型：
    - **进度型(metric)**：设置起始值和目标值，可逐步check-in更新当前值
    - **完成/未完成型(boolean)**：只有完成/未完成两种状态，点击切换
    - **任务型(task)**：关联看板任务，根据任务完成情况自动同步进度
- 里程碑区域：勾选式列表 + 添加输入框
- 关联任务区域：展示从看板关联的任务
- 进度日志/复盘区域：评分(1-5星) + 文字复盘

### 原型图3：新建目标弹窗（无标题2.png）
- 弹窗标题：「新建目标」
- 表单字段：
  - **目标名称**（必填，红色星号标识）
  - **目标周期**（必填，红色星号标识）— 下拉选择：
    - Q1 / Q2 / Q3 / Q4（季度）
    - 年度
    - 自定义（选日期范围）
  - **父目标**（可选）— 下拉选择已有目标
  - **关键结果**区域：
    - 可添加多个KR
    - 每个KR：名称 + 类型选择（进度/未完成完成/任务）
    - 进度型额外字段：起始值、目标值、单位
    - 任务型额外字段：关联任务选择
    - 可删除单个KR
- 底部：取消 + 创建按钮

---

## 二、与现有实现的差距分析

| 维度 | 现有实现 | 原型要求 | 差距 |
|------|---------|---------|------|
| **目标周期** | 仅`dueAt`一个日期 | 需要周期概念(Q1/Q2/Q3/Q4/年度/自定义) + startDate | **需新增`period`字段和周期选择UI** |
| **新建弹窗** | 简单表单(名称+描述+日期+颜色) | 完整表单(名称+周期+父目标+内联KR创建) | **需重构创建弹窗** |
| **KR类型** | metric/boolean/milestone | metric/boolean/task(任务型) | **需将milestone改为task，增加任务关联逻辑** |
| **KR在列表展示** | GoalsPage不显示KR | 每个目标卡片内显示KR摘要 | **需在GoalCard中增加KR列表** |
| **任务型KR** | 无任务关联UI | 可关联看板任务，自动同步进度 | **需新增任务关联UI和自动同步** |
| **进度颜色** | 仅在详情页有 | 列表页也要有 | **需在GoalCard中增加颜色逻辑** |
| **里程碑** | 独立管理 | 保留但弱化(原型中不突出) | 保留现有功能 |
| **描述字段** | 有description | 原型中未突出 | 可保留但不作为主要展示 |
| **颜色选择** | 创建时可选 | 原型中未突出 | 保留但移到详情编辑 |
| **复盘/评分** | 详情页有 | 原型中有 | 保留现有 |

---

## 三、重构产品规格

### 3.1 数据模型变更

#### Goal 新增/修改字段
```typescript
interface Goal {
  // ...existing fields...
  period: 'Q1' | 'Q2' | 'Q3' | 'Q4' | 'yearly' | 'custom';
  startDate?: string | null;  // 已有，用于custom周期
  // dueAt 保留，作为周期结束日期
}
```

#### KeyResult 类型变更
```typescript
interface KeyResult {
  // ...existing fields...
  type: 'metric' | 'boolean' | 'task';  // milestone → task
  // task型KR通过goal_tasks表关联任务
}
```

### 3.2 页面结构

#### GoalsPage（目标列表页）
- 顶部：标题 + 新建按钮
- 目标卡片列表：
  - 颜色竖条 + 名称 + 周期标签
  - 整体进度条 + 百分比
  - KR摘要列表（每个KR一行：类型图标+名称+迷你进度条+数值）
  - 子目标缩进
- 空状态

#### GoalDetailPage（目标详情页）
- 顶部：返回 + 颜色 + 名称(可编辑) + 周期 + 操作
- 进度总览卡片
- 关键结果区域（核心）
  - KR卡片列表，每个含：类型图标、标题、权重、进度条、数值、操作
  - 添加KR按钮
- 里程碑区域
- 关联任务区域
- 复盘区域

#### CreateGoalModal（新建目标弹窗）
- 目标名称（必填）
- 目标周期（必填，下拉：Q1/Q2/Q3/Q4/年度/自定义）
- 父目标（可选，下拉）
- 关键结果区域（可添加多个KR，每个含：名称+类型+类型特定字段）
- 取消 + 创建

### 3.3 周期系统设计

周期选项：
- **Q1**: 当年1月1日 ~ 3月31日
- **Q2**: 当年4月1日 ~ 6月30日
- **Q3**: 当年7月1日 ~ 9月30日
- **Q4**: 当年10月1日 ~ 12月31日
- **年度**: 当年1月1日 ~ 12月31日
- **自定义**: 用户选择startDate和dueAt

周期选择后自动计算startDate和dueAt，显示为标签如"Q2 2026"。

### 3.4 KR类型设计

| 类型 | 图标 | 行为 | 进度计算 |
|------|------|------|---------|
| 进度(metric) | 📊 TrendingUp | 设置起始值/目标值，可check-in更新 | (current-start)/(target-start) |
| 完成/未完成(boolean) | ✅ CheckCircle | 点击切换完成/未完成 | 完成=100%，未完成=0% |
| 任务(task) | 📋 ListTodo | 关联看板任务，自动同步 | 已完成任务数/总任务数 |

---

## 四、开发任务拆解

### 阶段1：后端数据模型变更（Rust + SQLite）

**任务1.1：数据库迁移 v5**
- goals表新增`period TEXT DEFAULT 'yearly'`字段
- key_results表的type字段默认值改为'metric'（已有）
- 无需新表，goal_tasks已存在

**任务1.2：Rust模型更新**
- Goal结构体新增`period: String`字段
- KeyResult的kr_type保持String（兼容metric/boolean/task/milestone）
- create_goal命令新增period参数
- update_goal命令新增period参数
- create_goal时根据period自动计算startDate和dueAt

**任务1.3：任务型KR进度计算**
- 修改calc_kr_progress：当kr_type="task"时，查询goal_tasks统计完成率
- 修改recalc_goal_progress：同上
- 新增link_task_to_kr / unlink_task_from_kr命令

### 阶段2：前端类型和API更新

**任务2.1：TypeScript类型更新**
- Goal/GoalWithDetails新增period字段
- KeyResult.type改为'metric' | 'boolean' | 'task'
- 新增周期相关类型

**任务2.2：API层更新**
- goalsApi.create新增period参数
- goalsApi.update新增period参数
- 新增linkTask/unlinkTask API

**任务2.3：Store更新**
- useGoalStore新增linkTask/unlinkTask actions
- createGoal参数新增period

**任务2.4：i18n更新**
- 新增周期相关翻译key（Q1/Q2/Q3/Q4/年度/自定义/目标周期等）
- KR类型翻译更新（milestone→task）

### 阶段3：新建目标弹窗重构

**任务3.1：CreateGoalModal组件**
- 目标名称输入（必填标识）
- 周期选择器（下拉：Q1/Q2/Q3/Q4/年度/自定义，自定义时显示日期范围选择）
- 父目标选择器（下拉）
- 内联KR创建区域：
  - 添加KR按钮
  - 每个KR：名称输入 + 类型选择Tab(进度/完成/任务)
  - 进度型：起始值+目标值+单位输入
  - 任务型：任务选择器（从看板选择任务）
  - 删除KR按钮
- 取消 + 创建按钮

### 阶段4：目标列表页重构

**任务4.1：GoalCard重构**
- 颜色竖条 + 名称 + 周期标签chip
- 整体进度条 + 百分比（带进度颜色）
- KR摘要列表：
  - 每个KR一行：类型图标 + 名称 + 迷你进度条 + 数值
  - 进度颜色根据时间对比
- 子目标缩进展示
- 删除确认

**任务4.2：GoalsPage整体**
- 使用新的CreateGoalModal
- 目标卡片使用重构后的GoalCard
- 空状态优化

### 阶段5：目标详情页重构

**任务5.1：头部区域**
- 周期标签显示
- 进度颜色状态指示

**任务5.2：KR卡片重构**
- 类型图标区分（TrendingUp/CheckCircle/ListTodo）
- 任务型KR：显示关联任务列表 + 关联/取消关联按钮
- Check-in弹窗优化

**任务5.3：添加KR弹窗重构**
- 类型选择改为三选一Tab（进度/完成/任务）
- 任务型：任务选择器

### 阶段6：收尾和测试

**任务6.1：OverviewPage适配**
- 确保总览页目标数据展示正常

**任务6.2：测试验证**
- 创建目标（各周期、各KR类型）
- KR check-in和进度更新
- 任务型KR关联和自动同步
- 子目标层级
- 归档/删除
