# 安全设计

## 密码

- 使用 Argon2 哈希。
- 不保存明文密码。

## 验证码

- 只保存验证码 hash。
- 设置过期时间。
- 需要增加邮箱和 IP 发送频率限制。

## Token

- access token 短期有效。
- refresh token 只保存 hash。
- 退出登录和设备移除时吊销 refresh token。

## 设备

远程移除设备不能保证删除离线设备上的本地数据。UI 必须明确告知用户。

## 管理后台

- 管理员接口必须检查 role。
- 后续增加管理员 2FA。
- 所有高风险操作写入审计日志。

