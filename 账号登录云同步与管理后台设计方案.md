# 账号登录、云同步与管理后台设计方案

最后更新：2026-07-05

## 1. 目标

本方案用于将光阶 Todo 从纯本地个人效率工具升级为支持账号体系、邮箱验证、云端同步和系统管理员后台的正式产品形态。

核心目标：

1. 支持邮箱 + 密码注册登录。
2. 只有完成邮箱验证码验证后，才允许开启云同步。
3. 手机验证码能力仅预留，不接入短信服务。
4. 除头像外，用户核心业务数据都支持同步。
5. 支持用户更换设备、退出登录、远程移除设备、本机数据清理等完整场景。
6. 增加系统管理员后台，方便查看系统运行情况和产品运营情况。
7. 保持当前桌面端本地优先体验，不能因为网络问题影响本地使用。

## 2. 技术栈

### 2.1 桌面客户端

- Tauri 2
- React 18
- TypeScript
- SQLite
- Zustand
- Tailwind CSS
- ECharts

客户端继续坚持本地优先。用户不登录或未验证邮箱时，仍然可以完整使用本地功能，只是不允许云同步。

### 2.2 后端服务

- Rust
- Axum
- Tokio
- SQLx
- MySQL 8
- Redis
- Argon2 密码哈希
- JWT + Refresh Token
- Lettre 邮件发送
- Tracing / tracing-subscriber
- tower-http
- uuid
- chrono
- validator
- utoipa，可选，用于 OpenAPI 文档

### 2.3 管理员后台前端

- React
- TypeScript
- Vite
- Ant Design
- React Router
- TanStack Query
- ECharts
- Zustand 或 Jotai

### 2.4 部署

- Docker Compose
- MySQL
- Redis
- Nginx
- HTTPS
- 后续可增加 Sentry、Prometheus、Grafana

## 3. 账号体系

### 3.1 注册

注册方式：

1. 用户输入邮箱和密码。
2. 后端创建账号。
3. 初始状态为邮箱未验证。
4. 后端发送邮箱验证码。
5. 用户可以登录和本地使用，但不能开启云同步。
6. 用户输入验证码完成邮箱验证。
7. 邮箱验证后允许云同步。

### 3.2 登录

登录方式：

- 邮箱 + 密码。

登录成功后返回：

- access token，短期有效。
- refresh token，长期有效。
- device id，当前设备标识。
- user profile。
- email verified 状态。

登录时不区分临时设备或常用设备。任何设备未来都可能因为换电脑、借电脑、维修、出售、送人等情况变成“事后看来是临时设备”。

### 3.3 邮箱验证

邮箱验证码用于：

- 验证邮箱。
- 重置密码。
- 更换邮箱，后续支持。

验证码规则：

- 只保存验证码 hash，不保存明文。
- 5 到 10 分钟过期。
- 同邮箱发送频率限制。
- 同 IP 发送频率限制。
- 验证成功后设置 `email_verified_at`。

### 3.4 手机验证码预留

数据库、接口和 UI 预留手机号字段，但第一版不接短信服务。

当用户尝试使用手机验证码时，返回：

```text
手机验证码暂未开放
```

## 4. 同步权限规则

允许同步必须同时满足：

1. 用户已登录。
2. 邮箱已验证。
3. 用户状态为 active。
4. 当前 token 有效。
5. 当前设备未被撤销。

邮箱未验证时：

- 允许登录。
- 允许本地使用。
- 不允许调用同步 API。
- 设置页显示“验证邮箱后开启同步”。

## 5. 同步范围

### 5.1 同步数据

- 看板
- 列表
- 任务
- 子任务
- 目标
- 关键结果
- 进度历史
- 番茄记录
- 复盘记录
- 日历事件
- 节假日日历配置
- 软件设置
- 个人资料文字信息
- 同步配置

### 5.2 不同步数据

- 头像
- 邮箱会议账号密钥
- OAuth token
- 本机缓存
- 更新安装包
- 本机设备路径
- 本机日志文件

邮箱授权信息继续保存在本机系统凭据，不进入云端。

## 6. 同步策略

第一版推荐实现全量快照同步，稳定后再升级为增量同步。

### 6.1 第一版：全量快照同步

云端保存用户当前完整快照：

```text
sync_snapshots
- id
- user_id
- snapshot_json
- version
- client_version
- device_id
- created_at
- updated_at
```

优点：

- 开发快。
- 验证简单。
- 适合第一版上线。
- 更容易保证不会漏同步数据。

### 6.2 后续：增量同步

后续升级为：

```text
sync_items
- id
- user_id
- entity_type
- entity_id
- payload_json
- version
- deleted
- updated_at
```

客户端每条可同步数据增加：

```text
sync_id
sync_version
sync_updated_at
sync_deleted
sync_dirty
```

默认冲突策略：

- 最后修改时间优先。
- 冲突详情页后续再做。

## 7. 登录后的数据处理

### 7.1 本机无数据，云端有数据

处理：

- 自动从云端恢复。
- 显示“正在恢复云端数据”。
- 恢复完成后进入软件。

### 7.2 本机有数据，云端无数据

处理：

- 提示是否将本机数据上传到云端。

选项：

- 上传本机数据到云端。
- 暂不上传，仅本机使用。

如果邮箱未验证，不允许上传。

### 7.3 本机有数据，云端也有数据

必须提示用户选择：

- 合并本机与云端数据，推荐。
- 使用云端数据覆盖本机。
- 使用本机数据覆盖云端。
- 取消登录同步，仅本机使用。

危险操作必须二次确认。

## 8. 设备管理与退出登录

### 8.1 重要原则

不要在登录时让用户选择临时设备或常用设备。

真实场景通常是事后发生变化：

- 用户换电脑。
- 用户把旧电脑送人。
- 用户卖掉旧电脑。
- 用户临时借用电脑。
- 用户离职交还公司电脑。
- 用户电脑维修。
- 用户设备丢失或损坏。

因此系统必须按照“任何设备未来都可能变成临时设备”来设计。

### 8.2 设备表

```text
user_devices
- id
- user_id
- device_name
- device_fingerprint
- platform
- app_version
- last_login_at
- last_sync_at
- revoked_at
- wipe_requested_at
- wiped_at
- created_at
- updated_at
```

### 8.3 用户端设备管理

设置页增加“已登录设备”：

```text
当前设备
- 设备名
- 系统
- 最近同步时间
- 最近登录时间

其他设备
- 设备名
- 最近在线
- 操作：移除此设备
```

支持操作：

- 重命名设备。
- 移除此设备。
- 退出全部其他设备。
- 当前设备退出登录。
- 当前设备退出并清除本机数据。

### 8.4 当前设备退出

点击退出登录后，不直接退出，先检查未同步数据。

如果有未同步数据且邮箱已验证：

```text
当前有未同步的数据。
请选择：
- 立即同步后退出
- 不同步，仅退出账号并保留本机数据
- 取消
```

如果邮箱未验证：

```text
邮箱未验证，无法同步。
退出后本机数据仍会保留，但不会上传到云端。
```

退出确认：

```text
退出登录
- 仅退出账号，保留本机数据，推荐
- 退出账号并清除此设备上的本地数据
- 取消
```

默认不删除本地数据。

### 8.5 清除此设备本地数据

如果用户选择“退出账号并清除此设备上的本地数据”，删除：

- 当前用户本机数据库。
- 当前用户 token。
- 当前用户 refresh token。
- 当前用户同步配置。
- 当前用户本地缓存。

可选清除：

- 本机邮箱授权信息。
- 本机头像缓存。

不删除：

- 云端数据。
- 应用安装文件。
- 其他用户本地数据。
- 全局默认设置。

危险操作二次确认：

```text
此操作只删除这台设备上的数据，不会删除云端数据。
请输入“清除”确认。
```

### 8.6 用户拿不到旧设备

如果用户已经拿不到旧设备，只能远程移除设备授权。

远程移除设备的效果：

- 后端撤销该设备 refresh token。
- 该设备不能继续同步。
- 该设备下次联网请求时被踢出。
- 后端记录设备被移除。

必须明确提示：

```text
移除此设备只能阻止它继续访问云端数据。
如果该设备上已有本地数据，系统无法远程保证删除。
```

### 8.7 远程请求清理本地数据

可以提供高级选项：

```text
[ ] 如果该设备再次联网，自动清除本账号本地数据
```

后端设置 `wipe_requested_at`。

旧设备下次联网后：

1. API 返回设备已撤销且有清理请求。
2. 客户端清理当前用户本机数据。
3. 客户端回传 `wiped_at`。

必须说明：

```text
清理请求只有在旧设备重新联网并启动软件后才会执行。
```

### 8.8 被远程移除设备的客户端行为

旧设备同步或请求接口时，如果 API 返回 `DEVICE_REVOKED`：

1. 立即停止同步。
2. 清除 token。
3. 不默认删除本地数据。
4. 显示“此设备已被移除，请重新登录。”
5. 如果存在 `wipe_requested_at`，执行本账号本机数据清理。

## 9. 本地多账号数据隔离

推荐使用每个账号独立 SQLite 文件。

```text
data/local.sqlite
data/users/{user_id}.sqlite
```

未登录时：

- 使用 `local.sqlite`。

登录后：

- 切换到 `users/{user_id}.sqlite`。

退出但保留数据：

- 用户数据库文件保留。
- token 清除。
- 下次同账号登录可继续使用。

退出并清除本机数据：

- 删除 `users/{user_id}.sqlite`。
- 清除该用户缓存。
- 清除 token。

优点：

- 多账号不混数据。
- 清理某个账号本机数据简单。
- 用户 A 退出后，用户 B 不会看到 A 的数据。
- 后续支持账号切换更安全。

## 10. 后端主要数据表

### 10.1 用户表

```text
users
- id
- email
- email_verified_at
- phone
- phone_verified_at
- password_hash
- nickname
- status
- role
- created_at
- updated_at
- last_login_at
```

### 10.2 邮箱验证码表

```text
email_verification_codes
- id
- user_id
- email
- code_hash
- purpose
- expires_at
- consumed_at
- send_ip
- created_at
```

### 10.3 Refresh Token 表

```text
refresh_tokens
- id
- user_id
- device_id
- token_hash
- expires_at
- revoked_at
- created_at
- last_used_at
```

### 10.4 同步日志表

```text
sync_logs
- id
- user_id
- device_id
- action
- status
- local_version
- remote_version
- error_message
- payload_size
- created_at
```

### 10.5 管理后台审计日志

```text
admin_audit_logs
- id
- admin_user_id
- action
- target_type
- target_id
- ip
- user_agent
- created_at
```

## 11. 管理员后台

### 11.1 第一版功能

- 管理员登录。
- 运营概览。
- 用户列表。
- 用户详情。
- 邮箱验证状态。
- 设备列表。
- 同步记录。
- 错误日志。
- 系统健康状态。
- 客户端版本分布。

### 11.2 运营指标

- 总用户数。
- 已验证邮箱用户数。
- 今日注册。
- 今日登录。
- 今日同步次数。
- 同步失败率。
- 活跃设备数。
- 客户端版本分布。
- 邮件发送成功率。

### 11.3 管理员权限

- 超级管理员。
- 运营管理员。
- 只读观察员。

管理员账号和普通用户账号可以共用 users 表，但必须用 role 严格区分权限；也可以后续独立 admin_users 表。

## 12. API 设计

### 12.1 Auth

```text
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
POST /api/auth/refresh
GET  /api/auth/me
```

### 12.2 邮箱验证

```text
POST /api/email/send-verification-code
POST /api/email/verify
POST /api/email/send-reset-password-code
POST /api/email/reset-password
```

### 12.3 设备管理

```text
GET    /api/devices
PATCH  /api/devices/:id
DELETE /api/devices/:id
POST   /api/devices/revoke-others
POST   /api/devices/:id/request-wipe
```

### 12.4 同步

```text
GET  /api/sync/status
POST /api/sync/push-snapshot
GET  /api/sync/pull-snapshot
GET  /api/sync/logs
```

### 12.5 管理后台

```text
POST /api/admin/login
GET  /api/admin/overview
GET  /api/admin/users
GET  /api/admin/users/:id
GET  /api/admin/devices
GET  /api/admin/sync-logs
GET  /api/admin/error-logs
GET  /api/admin/system-health
```

## 13. 客户端 UI 改造

新增：

- 登录页。
- 注册页。
- 邮箱验证页。
- 忘记密码页。
- 同步状态组件。
- 设备管理页。
- 退出登录确认弹窗。
- 清除本机数据二次确认弹窗。

设置页增加：

- 当前账号。
- 邮箱验证状态。
- 发送验证码。
- 输入验证码。
- 手动同步。
- 自动同步开关。
- 最近同步时间。
- 同步失败原因。
- 已登录设备。

同步状态必须清楚显示：

```text
已同步
正在同步
有 12 项未同步
同步失败
邮箱未验证，无法同步
此设备已被移除
```

## 14. 安全策略

必须实现：

- 密码使用 Argon2 哈希。
- 验证码只保存 hash。
- Refresh token 只保存 hash。
- HTTPS。
- 登录频率限制。
- 验证码发送频率限制。
- 同 IP 限制。
- 管理后台权限校验。
- 管理后台操作审计。
- 敏感配置使用环境变量。

不能承诺：

- 远程一定能删除离线旧设备上的本地数据。

需要明确告知用户：

- 远程移除设备只能阻止继续访问云端。
- 如果旧设备已有本地数据，必须在旧设备上执行“退出并清除本机数据”才最可靠。

## 15. 第一阶段开发顺序

1. 新建 Rust 后端项目。
2. 新建 MySQL migration。
3. 实现邮箱 + 密码注册登录。
4. 实现邮箱验证码发送和验证。
5. 客户端增加登录、注册、邮箱验证 UI。
6. 实现邮箱未验证禁止同步。
7. 实现设备记录和设备撤销。
8. 实现全量快照同步。
9. 实现退出登录前未同步检查。
10. 实现退出并清除本机数据。
11. 新建 React 管理后台。
12. 实现后台登录、用户列表、设备列表、同步日志、运营概览。

## 16. 增加后端和后台后的项目目录树

推荐采用单仓库多应用结构：

```text
Ascend-Todo/
├─ apps/
│  ├─ desktop/
│  │  ├─ src/
│  │  │  ├─ api/
│  │  │  ├─ components/
│  │  │  ├─ hooks/
│  │  │  ├─ i18n/
│  │  │  ├─ pages/
│  │  │  ├─ store/
│  │  │  ├─ styles/
│  │  │  ├─ types/
│  │  │  ├─ utils/
│  │  │  ├─ App.tsx
│  │  │  └─ main.tsx
│  │  ├─ src-tauri/
│  │  │  ├─ capabilities/
│  │  │  ├─ icons/
│  │  │  ├─ scripts/
│  │  │  ├─ src/
│  │  │  │  ├─ commands/
│  │  │  │  ├─ db.rs
│  │  │  │  ├─ error.rs
│  │  │  │  ├─ lib.rs
│  │  │  │  ├─ main.rs
│  │  │  │  ├─ models.rs
│  │  │  │  └─ sync_engine.rs
│  │  │  ├─ Cargo.toml
│  │  │  └─ tauri.conf.json
│  │  ├─ public/
│  │  ├─ package.json
│  │  ├─ vite.config.ts
│  │  └─ tsconfig.json
│  │
│  ├─ admin-web/
│  │  ├─ src/
│  │  │  ├─ api/
│  │  │  ├─ assets/
│  │  │  ├─ components/
│  │  │  ├─ hooks/
│  │  │  ├─ layouts/
│  │  │  ├─ pages/
│  │  │  │  ├─ DashboardPage.tsx
│  │  │  │  ├─ LoginPage.tsx
│  │  │  │  ├─ UsersPage.tsx
│  │  │  │  ├─ UserDetailPage.tsx
│  │  │  │  ├─ DevicesPage.tsx
│  │  │  │  ├─ SyncLogsPage.tsx
│  │  │  │  ├─ ErrorLogsPage.tsx
│  │  │  │  └─ SystemHealthPage.tsx
│  │  │  ├─ router/
│  │  │  ├─ store/
│  │  │  ├─ types/
│  │  │  ├─ utils/
│  │  │  ├─ App.tsx
│  │  │  └─ main.tsx
│  │  ├─ package.json
│  │  ├─ vite.config.ts
│  │  └─ tsconfig.json
│  │
│  └─ server/
│     ├─ Cargo.toml
│     ├─ migrations/
│     │  ├─ 0001_create_users.sql
│     │  ├─ 0002_create_email_codes.sql
│     │  ├─ 0003_create_devices.sql
│     │  ├─ 0004_create_sync_tables.sql
│     │  └─ 0005_create_admin_logs.sql
│     └─ src/
│        ├─ main.rs
│        ├─ config.rs
│        ├─ error.rs
│        ├─ state.rs
│        ├─ routes/
│        │  ├─ mod.rs
│        │  ├─ auth.rs
│        │  ├─ email.rs
│        │  ├─ devices.rs
│        │  ├─ sync.rs
│        │  ├─ admin.rs
│        │  └─ health.rs
│        ├─ services/
│        │  ├─ mod.rs
│        │  ├─ auth_service.rs
│        │  ├─ mail_service.rs
│        │  ├─ device_service.rs
│        │  ├─ sync_service.rs
│        │  └─ admin_service.rs
│        ├─ repositories/
│        │  ├─ mod.rs
│        │  ├─ user_repo.rs
│        │  ├─ device_repo.rs
│        │  ├─ sync_repo.rs
│        │  └─ log_repo.rs
│        ├─ models/
│        │  ├─ mod.rs
│        │  ├─ user.rs
│        │  ├─ device.rs
│        │  ├─ sync.rs
│        │  └─ admin.rs
│        ├─ middleware/
│        │  ├─ mod.rs
│        │  ├─ auth.rs
│        │  └─ admin_auth.rs
│        └─ utils/
│           ├─ crypto.rs
│           ├─ jwt.rs
│           ├─ rate_limit.rs
│           └─ time.rs
│
├─ packages/
│  └─ shared-types/
│     ├─ src/
│     └─ package.json
│
├─ deploy/
│  ├─ docker-compose.yml
│  ├─ nginx/
│  │  └─ nginx.conf
│  ├─ mysql/
│  │  └─ init.sql
│  └─ scripts/
│
├─ docs/
│  ├─ api.md
│  ├─ sync.md
│  ├─ security.md
│  └─ operations.md
│
├─ .github/
│  └─ workflows/
├─ README.md
└─ 账号登录云同步与管理后台设计方案.md
```

如果短期不想移动现有桌面端目录，也可以先保持当前根目录桌面端结构，只新增：

```text
server/
admin-web/
deploy/
docs/
```

后续稳定后再迁移为 `apps/desktop`。
