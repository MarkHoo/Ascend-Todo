// Repository modules are intentionally thin in the first implementation.
// SQL is currently colocated in route/service modules so the first cloud-sync
// version stays easy to audit. Move repeated queries here as the API grows.
